import React from "react";
import MenuCard from "../components/MenuCard";

const Home = () => {
  return (
    <div className="home">
      <MenuCard></MenuCard>
    </div>
  );
};

export default Home;
